# Interactive Box

Create a cursor-aligned bounding box around marked faces or the active object.

- **Description**: Fits a box that follows the cursor's rotation.
- **Controls**:
  - `Mouse Wheel`: Rotate box (cycle edge alignments).
  - `S`: Snap cursor to nearest element.
  - `F` / `LMB`: Mark faces to include in bounds.
